#!/usr/bin/env python

# configuation file

# PURPOSE: store defaults for parameters called by internal functions. 
# New users who have different defaults would benefit from not calling alternative parameters at every use. 

gitPath = "/data/davidlab/users/fsm/biolog/phenotypic-characterization"
parentPath = "/data/davidlab/users/fsm/biolog/"

measurement = ('Optical Density','OD')
od_wavelength = 620
plate_size = (8,12)
time_units = 'seconds'
